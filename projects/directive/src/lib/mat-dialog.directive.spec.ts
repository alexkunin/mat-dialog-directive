import { MatDialogDirective } from './mat-dialog.directive';

describe('MatDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new MatDialogDirective();
    expect(directive).toBeTruthy();
  });
});
